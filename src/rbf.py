def gaussian_rbf(x, mu, sigma):
    return ""


def design_matrix(X, mu, sigma):
    return ""


def train_least_squares(X, y, centers, sigma):
    return ""


def predict_rbf(X, centers, sigma, weights):
    return ""


def train_delta(X, y, centers, sigma, eta, epochs):
    return ""

